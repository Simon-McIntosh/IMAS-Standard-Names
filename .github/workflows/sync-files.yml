name: pending-delete-sync-project-files

# This file is a placeholder. It is safe to delete.
# If you have permissions, run:
#   git rm .github/workflows/sync-files.yml
# and commit the removal. Retained only because automated deletion
# via apply_patch did not remove it in this environment.

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This deprecated workflow will be removed."
