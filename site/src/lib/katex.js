// Shared KaTeX rendering options used by `<MathInline/>`. The CSS is
// imported once globally from `main.jsx`.
export const KATEX_OPTS = {
  throwOnError: false,
  errorColor: '#b94a48',
  strict: false,
  trust: true,
};
